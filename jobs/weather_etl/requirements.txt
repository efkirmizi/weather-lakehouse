# pyspark/py4j are pinned in the Dockerfile: the baked Iceberg/Nessie JARs
# live inside PySpark's install directory, so that layer must come first.
pandas==3.0.5
numpy==2.4.6
openmeteo-requests==1.7.5
openmeteo-sdk==1.28.0
requests-cache==1.3.3
retry-requests==2.0.0
